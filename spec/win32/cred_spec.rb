require 'spec_helper'

describe Win32::Cred do
  it 'has a version number' do
    expect(Win32::Cred::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
